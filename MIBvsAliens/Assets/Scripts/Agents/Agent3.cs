using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Agent3 : BaseAgent
{
    // Start is called before the first frame update
    void Start()
    {
        type = AgentType.Type3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
